# Core Dependencies
streamlit>=1.28.0
pandas>=2.0.0
python-dotenv>=1.0.0

# RAG & Embeddings (Local)
chromadb>=0.5.0
sentence-transformers>=2.2.0

# Fix dependency version conflicts (required for ChromaDB)
protobuf>=3.20.0
numpy>=1.24.0
